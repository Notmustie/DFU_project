# Core
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scikit-image>=0.21.0
scipy>=1.11.0

# Image processing
Pillow>=10.0.0

# Oversampling
imbalanced-learn>=0.11.0

# Notebook assembly
nbformat>=5.9.0

# Visualisation
matplotlib>=3.8.0

# Optional: GPU acceleration (install the version matching your CUDA)
# torch and torchvision above will install the CPU version by default.
# For CUDA 12.x:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For Apple Silicon (MPS):
#   pip install torch torchvision   (arm64 build ships MPS support)
